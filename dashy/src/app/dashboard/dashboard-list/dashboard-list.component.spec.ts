import {DashboardListComponent} from './dashboard-list.component';

describe('Component: DashboardList', () => {
  it('should create an instance', () => {
    let component = new DashboardListComponent();
    expect(component).toBeTruthy();
  });
});
