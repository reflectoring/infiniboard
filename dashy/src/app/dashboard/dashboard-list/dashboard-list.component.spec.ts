import {DashboardListComponent} from './dashboard-list.component';

describe('Component: DashboardList', () => {
  it('should create an instance', () => {
    const component = new DashboardListComponent();
    expect(component).toBeTruthy();
  });
});
